HTTP/1.1 200 OK
Content-Type: application/json

{{#code}}
(() => {
    const isConfirm = true ;
    return {
        status: 200,
        headers: {
            'X-Requested-By': 'user/confirm'
        },
        body: JSON.stringify({
            isConfirm: isConfirm,
        })
    };
})();
{{/code}}